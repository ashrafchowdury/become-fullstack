{
  "devDependencies": {
    "prettier": "3.0.3",
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "check-format": "prettier --check .",
    "format": "prettier --write ."
  }
}
